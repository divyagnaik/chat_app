STEPS TO EXECUTE :

Run the command ' npm start ' on one terminal to start the server .
 
And run the command 'npm run build ' on another terminal to execute the angular code .

Now open your browser and type 'localhost:3000' and there we have it our ONLINE CHAT APPLICATION named VCHAT.